<h2>New book</h2>
<%= form_with model: @book, url: books_path, local:true do |f| %>
  <h4>ショップ名</h4>
  <%= f.text_field :title %>
  <h4>説明</h4>
  <%= f.text_area :body %>
  <%= f.submit '投稿' %>
<% end %>